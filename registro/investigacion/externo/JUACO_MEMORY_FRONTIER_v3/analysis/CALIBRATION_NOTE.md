# Calibration note

The first sweep was non-discriminating because all architectures went extinct almost immediately. This is a simulator-dynamics confound, not a result about memory. The only change in this loop was metabolic pressure: initial energy/water 20 instead of 8 and per-round cost .35 instead of .7. No memory rule was changed.
